name: CI

on:
  push:
    branches: ["**"]
  pull_request:
  # Called by release.yml, so a tag runs exactly the checks a branch runs and
  # the two cannot drift apart.
  workflow_call:
  # Started by hand, and by homebrew-formula.yml on the branch it opens its
  # pull request from. GitHub starts no workflow run from a push or a pull
  # request that a GITHUB_TOKEN made, so a branch pushed by a workflow would
  # otherwise carry no checks at all. workflow_dispatch is the documented
  # exception to that rule, and this is what makes it available.
  workflow_dispatch:

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # /bin/bash on macOS is 3.2, which is what the tool must run under on
          # a machine with nothing installed. macOS is the only runner that
          # has the real thing, so this leg stays there.
          - name: system bash 3.2
            os: macos-latest
            bash: /bin/bash
          # Homebrew's bash, so a modern-bash regression is caught too.
          - name: latest bash
            os: macos-latest
            bash: brew
          # Linux is where most automation, and any future Go port, runs
          # first. Nothing exercised it before, so a GNU-versus-BSD drift in
          # stat, sed or find would have shipped unnoticed.
          - name: system bash, Linux
            os: ubuntu-latest
            bash: /bin/bash

    name: test (${{ matrix.name }})
    steps:
      - uses: actions/checkout@v4

      - name: Install latest bash
        if: matrix.bash == 'brew'
        run: brew install bash

      - name: Resolve the interpreter
        id: sh
        run: |
          if [ "${{ matrix.bash }}" = "brew" ]; then
            echo "path=$(brew --prefix)/bin/bash" >> "$GITHUB_OUTPUT"
          else
            echo "path=/bin/bash" >> "$GITHUB_OUTPUT"
          fi

      - name: Report the version under test
        run: |
          "${{ steps.sh.outputs.path }}" --version | head -1

      # Only the macOS system-bash leg is claiming to run the real 3.2; the
      # Linux system bash is a modern one and must not be held to that.
      - name: Fail if the 3.2 job is not actually running 3.2
        if: matrix.os == 'macos-latest' && matrix.bash != 'brew'
        run: |
          v=$(/bin/bash -c 'echo "$BASH_VERSION"')
          case "$v" in
            3.2*) echo "confirmed bash $v" ;;
            *) echo "expected bash 3.2 at /bin/bash, found $v"; exit 1 ;;
          esac

      - name: Syntax check
        run: |
          for f in bin/agent-profile tools/*.sh tests/run.sh tests/cases/*.sh; do
            "${{ steps.sh.outputs.path }}" -n "$f"
          done

      - name: Run the suite
        run: |
          set -o pipefail
          "${{ steps.sh.outputs.path }}" tests/run.sh | tee /tmp/test-output.txt

      # The suite's own "<n> test(s), <n> failure(s)" line and the count in
      # README.md's Development section must agree, or the two have drifted
      # and one of them is lying. tools/gen-test-count.sh is the one place
      # that parses both sides, so a contributor checking this locally and CI
      # checking it here can never disagree about what "agrees" means; a
      # number hard-coded into this workflow would itself become the next
      # thing to drift. --from reads the run above instead of running the
      # suite twice.
      - name: Test count matches README.md
        run: |
          "${{ steps.sh.outputs.path }}" tools/gen-test-count.sh --check --from /tmp/test-output.txt

      # Every command example the documentation shows is generated from a
      # real run of the tool in a throwaway HOME, and this fails naming the
      # example that no longer matches what the tool prints. It runs on every
      # leg, so an output that differs between bash 3.2 and a modern bash, or
      # between macOS and Linux, is caught here rather than read by someone.
      - name: Documented examples match the tool
        run: |
          "${{ steps.sh.outputs.path }}" tools/gen-doc-examples.sh --check

  # The sessions server under server/ is Python, tested with pytest against
  # generated fixtures. It runs on both platforms the tool ships to, from the
  # lockfile the release ships, so a dependency that resolves differently on a
  # Mac is caught here rather than at a coworker's install.
  server:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    name: server (${{ matrix.os }})
    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v6

      - name: Install the locked environment
        working-directory: server
        run: uv sync --frozen --group dev

      - name: Run the server tests
        working-directory: server
        run: uv run --frozen --group dev pytest -q

      # What the installer does on a Mac with uv, against the same lockfile,
      # and what the launcher then execs. A broken lockfile fails here.
      - name: Build the environment the way the installer does
        working-directory: server
        run: |
          rm -rf .venv
          uv sync --frozen --no-dev --no-install-project
          test -x .venv/bin/python
          CLAUDE_CONFIG_DIR=/tmp/none PYTHONPATH=. .venv/bin/python -m agent_profile_sessions --root /tmp/none 2>&1 | grep -q "refusing to start"

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install shellcheck
        run: |
          sudo apt-get update
          sudo apt-get install -y shellcheck

      - name: shellcheck
        run: shellcheck bin/agent-profile tools/*.sh tests/run.sh tests/cases/*.sh

      - name: Refuse bash 4 constructs
        run: ./tools/lint-bash32.sh bin/agent-profile tools/probe-claude-desktop.sh tools/gen-test-count.sh tools/gen-doctor-reads.sh tools/gen-doc-examples.sh tools/update-homebrew-formula.sh tests/run.sh
